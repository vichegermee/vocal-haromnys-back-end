package com.vocalharmonys.backend.entity;

/**
 * The audio tracks offered for a repertoire song: the four voice parts, plus
 * a full-choir mix and an instrumental-only version.
 */
public enum TrackType {
    SOPRANO,
    ALTO,
    TENOR,
    BASSE,
    CHOEUR,
    INSTRUMENTAL
}
